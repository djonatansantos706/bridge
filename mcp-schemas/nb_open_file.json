{
  "name": "nb_open_file",
  "description": "Abre um arquivo no editor do NetBeans em uma linha específica.",
  "parameters": {
    "type": "object",
    "properties": {
      "file": {
        "type": "string",
        "description": "Caminho absoluto do arquivo a ser aberto"
      },
      "line": {
        "type": "integer",
        "description": "Número da linha (opcional, padrão 1)"
      },
      "take_focus": {
        "type": "boolean",
        "description": "Se deve ativar a aba e focar o editor (padrão false para não roubar foco do usuário)"
      }
    },
    "required": [
      "file"
    ]
  }
}
